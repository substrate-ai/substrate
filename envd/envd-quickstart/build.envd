def build():
    base(os="ubuntu20.04", language="python3.10")
    
    # Configure pip index if needed.
    # config.pip_index(url = "https://pypi.tuna.tsinghua.edu.cn/simple")
    install.python_packages(name = [
        # "torch",
        # "torchvision",
    ])
    config.gpu(count=1)
    runtime.init(["python3.10", "./demo.py"])
    
